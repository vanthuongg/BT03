# Project Sequelize + TypeScript

## Cách chạy

Đầu tiên clone project về máy:
```bash
git clone <repo-url>
cd app
````

Tiếp theo cài đặt dependencies:

```bash
npm install
```

Sau đó chạy project:

```bash
npm start
```
